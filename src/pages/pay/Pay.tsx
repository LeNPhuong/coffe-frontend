import React from "react";
import PayMain from "../../components/pay/PayMain";

const Pay: React.FC = () => {
  window.scrollTo(0, 0);
  return (
    <section>
      <PayMain />
    </section>
  );
};

export default Pay;
